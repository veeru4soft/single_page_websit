<%= form_for(@styling_page) do |f| %>
  <% if @styling_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@styling_page.errors.count, "error") %> prohibited this styling_page from being saved:</h2>

      <ul>
      <% @styling_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :font %><br>
    <%= f.text_field :font %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
